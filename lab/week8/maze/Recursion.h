#include <stdio.h>
#include <stdlib.h>

typedef struct
{

} maze;

void FindMaze(int xi, int yi, int xe, int ye, maze maze)
{
    int curX = xi, curY = yi;
    // base case
    if (curX == xe && curY == ye)
    {
        // what should the base case return?
    }
}